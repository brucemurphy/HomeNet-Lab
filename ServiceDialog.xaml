<Window x:Class="ServiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Service Editor"
        Height="350"
        Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <!-- Dialog Button Style with Rounded Corners -->
        <Style x:Key="DialogButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#BF000000" CornerRadius="10" Padding="40,30" BorderBrush="#FF555555" BorderThickness="1">
        <Grid>
            <!-- Close button at top right -->
            <Button Content="✕" 
                    Click="CancelButton_Click"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="0,-15,-15,0"
                    Background="Transparent" 
                    Foreground="#FFAAAAAA" 
                    BorderThickness="0"
                    FontSize="20"
                    Cursor="Hand"
                    Width="30"
                    Height="30"
                    ToolTip="Close (Esc)">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFAAAAAA"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel>
                <TextBlock Text="🔌 Service Configuration" FontSize="22" Foreground="#FFCCCCCC" Margin="0,0,0,25" HorizontalAlignment="Center"/>

                <TextBlock Text="Service Name:" Foreground="#FFAAAAAA" Margin="0,8,0,5" FontSize="13"/>
                <TextBox x:Name="txtServiceName" Background="#FF2E2E2E" Foreground="White" Padding="10" BorderBrush="#FF555555" BorderThickness="1" Margin="0,0,0,15" FontSize="13"/>

                <TextBlock Text="Port:" Foreground="#FFAAAAAA" Margin="0,8,0,5" FontSize="13"/>
                <TextBox x:Name="txtServicePort" Background="#FF2E2E2E" Foreground="White" Padding="10" BorderBrush="#FF555555" BorderThickness="1" Margin="0,0,0,15" FontSize="13"/>

                <CheckBox x:Name="chkServiceEnabled" Content="Enabled" Foreground="#FFAAAAAA" IsChecked="True" Margin="0,8,0,25" FontSize="13"/>

                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="💾 Save" Click="OkButton_Click" 
                            Style="{StaticResource DialogButton}"
                            Background="#CC555555" Padding="12" FontSize="12" IsDefault="True"/>
                    <Button Grid.Column="2" Content="❌ Cancel" Click="CancelButton_Click" 
                            Style="{StaticResource DialogButton}"
                            Background="#CC555555" Padding="12" FontSize="12" IsCancel="True"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
